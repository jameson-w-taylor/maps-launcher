import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ios-maps-link',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
